import { ColorOpacityDirective } from './color-opacity.directive';

describe('ColorOpacityDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorOpacityDirective();
    expect(directive).toBeTruthy();
  });
});
